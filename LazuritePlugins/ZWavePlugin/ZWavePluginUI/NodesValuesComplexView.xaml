<UserControl x:Class="ZWavePluginUI.NodesValuesComplexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZWavePluginUI"
             xmlns:controls="clr-namespace:LazuriteUI.Windows.Controls;assembly=LazuriteUI.Windows.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <local:NodesListView Margin="10" BorderBrush="{x:Static controls:Visual.BorderBrush}" BorderThickness="2" Grid.Column="0" x:Name="nodesListView"></local:NodesListView>
        <local:NodeValuesListView Margin="10" BorderBrush="{x:Static controls:Visual.BorderBrush}" BorderThickness="2" Grid.Column="1" x:Name="valuesListView"></local:NodeValuesListView>
    </Grid>
</UserControl>
