<UserControl x:Class="LazuriteUI.Windows.Main.Switches.SwitchSettings.SwitchIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazuriteUI.Windows.Main.Switches.SwitchSettings"
             xmlns:controls="clr-namespace:LazuriteUI.Windows.Controls;assembly=LazuriteUI.Windows.Controls"
             xmlns:switches="clr-namespace:LazuriteUI.Windows.Main.Switches"
             mc:Ignorable="d"
             Height="60"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <switches:StringToIcon x:Key="stringToIcon"></switches:StringToIcon>
        </Grid.Resources>
        <controls:ItemView x:Name="itemView" Selectable="True" Selected="{Binding ItemSelected, Mode=TwoWay}" VerticalAlignment="Stretch" Height="60" Icon="{Binding Icon, Converter={StaticResource stringToIcon}}" Content="{Binding Icon}"></controls:ItemView>
    </Grid>
</UserControl>
