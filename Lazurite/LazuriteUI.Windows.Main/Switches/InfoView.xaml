<UserControl x:Class="LazuriteUI.Windows.Main.Switches.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazuriteUI.Windows.Main.Switches"
             mc:Ignorable="d" 
             xmlns:controls="clr-namespace:LazuriteUI.Windows.Controls;assembly=LazuriteUI.Windows.Controls"
             Height="80" Width="80"
             Background="{x:Static controls:Visual.BackgroundLazurite}"
             d:DesignHeight="80" d:DesignWidth="80">
    <Grid>
        <Grid.Resources>
            <local:StringToIcon x:Key="stringToIcon"></local:StringToIcon>
        </Grid.Resources>
        <Grid IsHitTestVisible="False">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <controls:IconView Icon="{Binding Icon1, Converter={StaticResource stringToIcon}, Mode=TwoWay}"></controls:IconView>
            <Viewbox Grid.Row="1" VerticalAlignment="Top" Margin="3,0,3,0">
                <TextBlock Name="textBlockScenName" Text="{Binding ScenarioName}" x:FieldModifier="private" Foreground="SteelBlue" FontSize="8.5"></TextBlock>
            </Viewbox>
            <Viewbox Grid.Row="2" VerticalAlignment="Top" Margin="3,0,3,3">
                <TextBlock Name="textBlockScenValue" Text="{Binding ScenarioValue}" x:FieldModifier="private" Foreground="White" VerticalAlignment="Top" TextAlignment="Center" FontSize="8.5"></TextBlock>
            </Viewbox>
        </Grid>
    </Grid>
</UserControl>