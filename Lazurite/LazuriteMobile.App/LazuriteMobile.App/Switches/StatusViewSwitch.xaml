<?xml version="1.0" encoding="UTF-8"?>
<Grid 
    x:Class="LazuriteMobile.App.Switches.StatusViewSwitch"
    xmlns:controls="clr-namespace:LazuriteMobile.App.Controls"
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:LazuriteMobile.App.Switches"
    BackgroundColor="{Static controls:Visual.Background}"
    Padding="4"
    Margin="4">

    <Grid.RowDefinitions>
        <RowDefinition Height="40"></RowDefinition>
        <RowDefinition Height="Auto"></RowDefinition>
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="30"></ColumnDefinition>
        <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>

    <controls:IconView IsVisible="False" x:Name="iconTitle" Opacity="0.64" VerticalOptions="Center" Icon="{Binding Icon1, Converter={Static local:ConvertersStatic.StringToIcon}, Mode=TwoWay}"></controls:IconView>
    <Label IsVisible="False" VerticalTextAlignment="Center" x:Name="lblTitle" Grid.Column="1" TextColor="{Static controls:Visual.CaptionForeground}" Text="{Binding ScenarioName, Converter={Static local:ConvertersStatic.StringToShortStringBig}}"></Label>

    <controls:IconView IsVisible="False" x:Name="iconSearch" Opacity="0.4" VerticalOptions="Center" Icon="Magnify"></controls:IconView>
    <controls:ItemView IsVisible="False" x:Name="btClearSearch" Icon="Close"></controls:ItemView>
    <Entry IsVisible="False" VerticalOptions="Center" HorizontalOptions="Fill" Grid.Column="1" FontSize="Small" FontFamily="{Static controls:Visual.FontFamily}" Margin="0,0,0,-2" TextColor="White" x:Name="tbSearch"></Entry>

    <ListView Grid.Row="1" RowHeight="42" x:Name="listView" Grid.ColumnSpan="2"></ListView>
</Grid>